<section id="booking" class="booking-section section-padding">
    <div class="container">
        <h2>AGENDA TU TURNO</h2>
        <p class="subtitle">La excelencia requiere tiempo. Reserva tu espacio exclusivo.</p>
        
        <div class="booking-placeholder-box">
            <h3>SISTEMA DE GESTIÓN DE TURNOS</h3>
            <p class="info-text">
                Próximamente podrás visualizar el calendario en tiempo real y reservar ingresando tu Nombre y Apellido.
            </p>
            
            <a href="#" class="btn btn-outline">
                Contactar por WhatsApp (Temporal)
            </a>
        </div>
    </div>
</section>

<style>
    .booking-section {
        background-color: var(--color-text-black);
        color: var(--color-bg-beige);
        text-align: center;
    }

    h2 {
        font-size: 3rem;
        margin-bottom: 10px;
        color: var(--color-bg-beige);
    }

    .subtitle {
        font-size: 1.1rem;
        opacity: 0.8;
        margin-bottom: 40px;
    }
    
    .booking-placeholder-box {
        border: 2px dashed rgba(242, 240, 230, 0.3); /* Borde sutil */
        background-color: rgba(255, 255, 255, 0.05); /* Fondo apenas visible */
        padding: 60px 20px;
        max-width: 600px;
        margin: 0 auto;
        border-radius: 4px;
    }

    .booking-placeholder-box h3 {
        font-size: 1.5rem;
        letter-spacing: 2px;
        margin-bottom: 20px;
    }

    .info-text {
        margin-bottom: 30px;
        font-size: 0.9rem;
        opacity: 0.7;
        max-width: 80%;
        margin-left: auto;
        margin-right: auto;
    }

    /* Estilo especial para botón sobre fondo oscuro */
    .btn-outline {
        border-color: var(--color-bg-beige);
        color: var(--color-bg-beige);
        background: transparent;
    }

    .btn-outline:hover {
        background-color: var(--color-bg-beige);
        color: var(--color-text-black);
    }
</style>