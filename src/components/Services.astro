<section class="hero">
    <div class="container">
        <div class="hero-content">
            <h1>
                <span>ESTUDIO PRIVADO</span>
                BARBERÍA<br>CLÁSICA &<br>MODERNA
            </h1>
            <p>
                Atención personalizada y exclusiva por Ezequiel Marin. 
                Más que un corte, una experiencia de confianza y estilo en un ambiente privado.
            </p>
            <a href="#booking" class="btn">Agendar Cita</a>
        </div>
    </div>
    <div class="hero-image-container"></div>
</section>

<style>
    .hero {
        height: 100vh; /* Ocupa toda la pantalla */
        min-height: 600px; /* Evita que se achique demasiado en pantallas apaisadas */
        display: flex;
        align-items: center;
        position: relative;
    }

    .hero-content {
        z-index: 2;
        max-width: 600px;
        position: relative;
    }

    h1 {
        font-size: 4rem;
        line-height: 1.1;
        margin-bottom: 20px;
    }

    h1 span {
        display: block;
        font-size: 1.5rem;
        font-weight: 400;
        letter-spacing: 5px;
        color: #555;
    }

    p {
        margin-bottom: 30px; 
        font-size: 1.1rem;
        max-width: 90%;
    }

    .hero-image-container {
        position: absolute;
        top: 0;
        right: 0;
        width: 50%; /* Mitad de pantalla en PC */
        height: 100%;
        background-image: url('https://images.unsplash.com/photo-1585747860715-2ba37e788b70?ixlib=rb-4.0.3&auto=format&fit=crop&w=1474&q=80');
        background-size: cover;
        background-position: center;
        z-index: 1;
    }

    /* --- RESPONSIVE (Móvil y Tablet) --- */
    @media (max-width: 900px) {
        .hero-image-container {
            width: 100%; /* Imagen ocupa todo el ancho */
            height: 100%;
            opacity: 0.15; /* Muy transparente para leer el texto encima */
            filter: grayscale(100%); /* Opcional: blanco y negro elegante */
        }

        .hero h1 { 
            font-size: 3rem; 
        }

        .hero h1 span {
            font-size: 1rem;
            letter-spacing: 3px;
        }

        .hero { 
            /* Centrar contenido en móvil */
            justify-content: center; 
            text-align: center;
        }

        .hero-content {
            margin: auto;
        }
    }
    
    @media (max-width: 480px) {
        .hero h1 { font-size: 2.5rem; }
    }
</style>