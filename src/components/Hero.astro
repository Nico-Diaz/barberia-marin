<section class="hero">
    <div class="container">
        <div class="hero-content">
            <h1>
                <span>ESTUDIO PRIVADO</span>
                BARBERÍA<br>CLÁSICA &<br>MODERNA
            </h1>
            <p>
                Atención personalizada y exclusiva por Ezequiel Marin. 
                Más que un corte, una experiencia de confianza y estilo en un ambiente privado.
            </p>
            <a href="#booking" class="btn">Agendar Cita</a>
        </div>
    </div>
    <div class="hero-image-container"></div>
</section>

<style>
    /* --- Estilos Base (Pantallas Grandes / PC) --- */
    .hero {
        height: 100vh; /* Ocupa toda la pantalla */
        min-height: 600px; /* Evita que se aplaste en pantallas bajitas */
        display: flex;
        align-items: center;
        position: relative;
        overflow: hidden; /* Asegura que nada se salga del contenedor */
    }

    .hero-content {
        z-index: 2;
        max-width: 600px;
        position: relative;
        padding-right: 40px; /* Un poco más de aire entre texto e imagen */
    }

    h1 {
        font-size: 4rem;
        line-height: 1.1;
        margin-bottom: 20px;
        color: var(--color-text-black);
    }

    h1 span {
        display: block;
        font-size: 1.5rem;
        font-weight: 400;
        letter-spacing: 5px;
        color: #555;
    }

    p {
        margin-bottom: 30px;
        font-size: 1.1rem;
        max-width: 90%;
    }

    .hero-image-container {
        position: absolute;
        top: 0;
        right: 0;
        width: 50%; /* Mitad derecha de la pantalla */
        height: 100%;
        background-image: url('https://images.unsplash.com/photo-1585747860715-2ba37e788b70?ixlib=rb-4.0.3&auto=format&fit=crop&w=1474&q=80');
        background-size: cover;
        /* AQUÍ ESTÁ EL CAMBIO CLAVE: */
        /* Usamos 'top center' para que la imagen se alinee arriba y no se recorte la cabeza/parte superior */
        background-position: top center;
        z-index: 1;
    }

    /* --- RESPONSIVE (Tablets y Móviles - menos de 900px) --- */
    @media (max-width: 900px) {
        .hero-image-container {
            width: 100%; /* La imagen ocupa todo el ancho */
            height: 100%;
            opacity: 0.15; /* Se hace transparente para que se lea el texto encima */
            background-position: center; /* En móvil suele quedar mejor centrada de nuevo */
        }

        .hero h1 {
            font-size: 3rem;
        }

        .hero h1 span {
            font-size: 1rem;
            letter-spacing: 3px;
        }

        .hero {
            /* Centrar contenido en móvil */
            justify-content: center;
            text-align: center;
            padding-top: 80px; /* Espacio para que el header no tape el título */
            align-items: flex-start; /* Alineamos arriba en móvil */
        }

        .hero-content {
            margin: auto;
            padding-right: 0;
            padding-top: 40px;
        }

        p {
            margin-left: auto;
            margin-right: auto;
        }
    }

    /* Móviles muy pequeños */
    @media (max-width: 480px) {
        .hero h1 { font-size: 2.5rem; }
    }
</style>